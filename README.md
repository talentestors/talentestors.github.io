## hexo blog

```shell
npm install hexo-cli -g
```

- - 环境：

    - npm i

    运行：

    - hexo server
  
    清理：
  
    - hexo clean
  
    编译：
  
    - hexo g

```shell
hexo clean && hexo generate && hexo backup && hexo deploy
```
